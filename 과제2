#include <stdio.h>
#include <string.h>

int main() {

	printf("문자열을 입력하세요:");
	char str[50];
	scanf_s("%s", str,sizeof(str));
	printf("입력한 문자열은\n\"%s\"\n입니다\n", str);
	printf("입력된 문자열의 길이:%zu\n", strlen(str));
  return 0;
}
