#include <stdio.h>
#include <string.h>

int main() {

 	char array[2][3][20];
	char* label[3] = { "이름","학과","학번" };
	for (int i = 0; i < 2; i++) {
			printf("학생%d의이름:",i+1);
			scanf_s("%s", array[i][0], sizeof(array[i][0]));
			printf("학생%d의학과:", i + 1);
			scanf_s("%s", array[i][1], sizeof(array[i][1]));
			printf("학생%d의학번:", i + 1);
			scanf_s("%s", array[i][2], sizeof(array[i][2]));
	}
	for (int i = 0; i < 2; i++) {
		printf("\n학생%d\n\t", i + 1);
		for (int j = 0; j < 3; j++) {
			for (int k = 0; array[i][j][k] != '\0'; k++) {
				printf("%c", array[i][j][k]);
			}
			printf("\n\t");
		}
	}
